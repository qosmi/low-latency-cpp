# Sample data

Sample detector data will be added in a later phase.

The simulator currently generates deterministic synthetic events in memory so that the initial repository has no large binary assets.
