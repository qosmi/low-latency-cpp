# Benchmarks

Performance benchmarks will be added after the baseline implementation is established.

The baseline is intentionally simple so that later optimizations can be compared against a known reference.
